# analysis_options.yaml
# Strict linting for Politiface.
# Every rule here either catches real bugs or enforces consistency.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    missing_required_param: error
    missing_return: error
    dead_code: warning
    unused_import: error
    unused_local_variable: warning
  exclude:
    - "**/*.g.dart"      # generated Drift + Riverpod files
    - "**/*.freezed.dart"

linter:
  rules:
    # ── Error prevention ──────────────────────────────────────────────────────
    # avoid_returning_null_for_future — removed in Dart 3.3
    - avoid_slow_async_io
    - cancel_subscriptions
    - close_sinks
    - literal_only_boolean_expressions
    - no_adjacent_strings_in_list
    - test_types_in_equals
    - throw_in_finally
    - unnecessary_statements

    # ── Performance ───────────────────────────────────────────────────────────
    # avoid_function_closures_in_foreach — not a recognized rule
    - prefer_for_elements_to_map_fromIterable
    - avoid_unnecessary_containers
    - sized_box_for_whitespace
    - use_full_hex_values_for_flutter_colors

    # ── Style consistency ──────────────────────────────────────────────────────
    - always_declare_return_types
    - always_put_required_named_parameters_first
    - annotate_overrides
    - avoid_bool_literals_in_conditional_expressions
    # avoid_catches_without_on_clauses — intentionally off. The codebase uses
    # bare `catch (_)` for best-effort side work (pending-session persistence,
    # node-unlock recalc, share flows) where ANY failure must not break the
    # user path; typed on-clauses would silently narrow that contract.
    # avoid_dynamic_calls — intentionally off. YAML/JSON parsing (content
    # loaders, Wikipedia client) is inherently dynamic; the parse layers
    # validate and cast at the boundary instead.
    - avoid_empty_else
    - avoid_print                 # use Sentry/logging instead
    - avoid_redundant_argument_values
    - avoid_relative_lib_imports
    - avoid_returning_this
    - avoid_setters_without_getters
    - avoid_shadowing_type_parameters
    - avoid_type_to_string
    # avoid_unnecessary_null_assertions — not a recognized rule
    - avoid_void_async
    - camel_case_extensions
    - camel_case_types
    - constant_identifier_names
    - curly_braces_in_flow_control_structures
    - directives_ordering
    - eol_at_end_of_file
    - file_names
    - library_prefixes
    - no_duplicate_case_values
    - no_runtimeType_toString
    - null_closures
    - omit_local_variable_types
    - one_member_abstracts
    - only_throw_errors
    - package_prefixed_library_names
    - prefer_asserts_in_initializer_lists
    - prefer_collection_literals
    - prefer_conditional_assignment
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - prefer_constructors_over_static_methods
    - prefer_contains
    - prefer_expression_function_bodies
    - prefer_final_fields
    - prefer_final_in_for_each
    - prefer_final_locals
    - prefer_if_null_operators
    - prefer_initializing_formals
    - prefer_inlined_adds
    - prefer_int_literals
    - prefer_is_empty
    - prefer_is_not_empty
    - prefer_is_not_operator
    - prefer_null_aware_operators
    - prefer_single_quotes
    - prefer_spread_collections
    - prefer_typing_uninitialized_variables
    - recursive_getters
    - require_trailing_commas
    - sort_child_properties_last
    - type_init_formals
    - unnecessary_await_in_return
    - unnecessary_brace_in_string_interps
    - unnecessary_const
    - unnecessary_getters_setters
    - unnecessary_lambdas
    - unnecessary_new
    - unnecessary_null_aware_assignments
    - unnecessary_null_checks
    - unnecessary_overrides
    - unnecessary_parenthesis
    - unnecessary_raw_strings
    - unnecessary_string_escapes
    - unnecessary_string_interpolations
    - unnecessary_this
    - use_colored_box
    - use_enums
    - use_if_null_to_convert_nulls_to_bools
    - use_is_even_rather_than_modulo
    - use_late_for_private_fields_and_variables
    - use_named_constants
    - use_raw_strings
    - use_rethrow_when_possible
    - use_setters_to_change_properties
    - use_string_buffers
    - use_super_parameters
    - use_test_throws_matchers
    - void_checks
